doubleMe x = x + x

doubleUs x y = (x + x) + (y + y)

multiplyMe x = x * x

addUs x y = x + y

subtractUs x y = x - y

divideUs x y = x / y

multiplyUs x y = x * y

doubleSmallNumber x = if (x > 100 then x else x*2)
